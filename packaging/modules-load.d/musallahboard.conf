# MusallahBoard: filesystem drivers for USB update sticks. Loaded at boot
# because the USB import helper runs with ProtectKernelModules=yes and cannot
# load them itself. vfat and ext4 are loaded by the base system already.
exfat
ntfs3
